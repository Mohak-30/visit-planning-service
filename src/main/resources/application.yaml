spring:
  application:
    name: VPS

server:
  port: 8081


eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8166/eureka

endpoints:
  health:
    enabled: true
    sensitive: false